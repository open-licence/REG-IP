# Настройка плавающих IP (Floating IP)

Интерактивный скрипт для простой настройки маршрутизации плавающих IP-адресов на Linux-серверах у хостера REG.RU.

## Возможности

- **Интерактивное меню** — простой выбор действий
- **Автоматическое определение** — интерфейсы, IP-адреса, шлюзы
- **Поиск свободных таблиц** — автоматически находит незанятые номера (1-252)
- **Управление правилами** — добавление, удаление, просмотр
- **Проверка связи** — встроенный ping для тестирования
- **Автозагрузка** — создание systemd сервиса для сохранения после перезагрузки

## Быстрый запуск (одной командой)

Выполните эту команду **от пользователя root**:

```bash
wget -O /usr/local/bin/fip-setup.sh https://raw.githubusercontent.com/open-licence/REG-IP/refs/heads/main/fip-setup.sh && chmod +x /usr/local/bin/fip-setup.sh && /usr/local/bin/fip-setup.sh
```

## Пошаговая установка

### 1. Скачайте скрипт

```bash
wget -O /usr/local/bin/fip-setup.sh https://raw.githubusercontent.com/open-licence/REG-IP/refs/heads/main/fip-setup.sh
```

### 2. Сделайте скрипт исполняемым

```bash
chmod +x /usr/local/bin/fip-setup.sh
```

### 3. Запустите скрипт

```bash
sudo /usr/local/bin/fip-setup.sh
```
*Примечание: скрипт сам проверит, что запущен от root*

## Использование

После запуска вы увидите интерактивное меню:

```
==================================================
    НАСТРОЙКА ПЛАВАЮЩИХ IP-АДРЕСОВ
==================================================

Выберите действие:
1) Настроить новый плавающий IP
2) Показать текущие правила
3) Удалить правило для IP
4) Проверить работу IP
5) Сохранить настройки в systemd
6) Выход
```

### Меню 1 — Настройка нового IP
Скрипт покажет доступные интерфейсы, попросит выбрать нужный, автоматически определит:
- IP-адрес интерфейса
- Шлюз подсети (например, 192.168.0.1)
- Свободный номер таблицы маршрутизации

После подтверждения добавит правила и проверит пинг.

### Меню 2 — Просмотр правил
Показывает все активные правила и соответствующие таблицы маршрутизации.

### Меню 3 — Удаление
Удаляет правила для указанного IP-адреса.

### Меню 4 — Проверка
Тестирует пинг для одного или всех настроенных IP.

### Меню 5 — Сохранение в systemd
Создаёт сервис для автоматического применения правил после перезагрузки:
- Скрипт автозагрузки: `/etc/network-fip-rules.sh`
- Конфигурация: `/etc/fip-rules.conf`
- Systemd сервис: `fip-routing.service`

## 📁 Структура файлов

| Файл | Назначение |
|------|------------|
| `/usr/local/bin/fip-setup.sh` | Основной скрипт настройки |
| `/etc/fip-rules.conf` | Сохранённые правила (создаётся автоматически) |
| `/etc/network-fip-rules.sh` | Скрипт для автозагрузки |
| `/etc/systemd/system/fip-routing.service` | Systemd сервис |
| `/var/log/fip-routing.log` | Лог выполнения автозагрузки |

## 🔧 Пример работы

```
Текущие сетевые интерфейсы:
ens3   UP   192.168.0.10/24
ens4   UP   192.168.0.11/24

Выберите номер интерфейса: 1
Выбран интерфейс: ens3
Его IP адрес: 192.168.0.10
Шлюз подсети: 192.168.0.1
Свободная таблица: 1

Будут выполнены команды:
ip rule add from 192.168.0.10 table 1 prio 1
ip route add default via 192.168.0.1 dev ens3 table 1

Продолжить? (y/n): y
✓ Правила успешно добавлены
✓ IP 192.168.0.10 успешно пингуется
```

## ⚠️ Требования

- **ОС:** Linux (протестировано на Ubuntu 24.04)
- **Права:** Доступ к root (через sudo или от root)
- **Команды:** `ip`, `ping`, `systemctl` (должны быть установлены)

## 📝 Лицензия

MIT
-- ## 🤝 Вклад в развитие
--
-- Если нашли баг или есть предложения — создавайте Issue или Pull Request.
